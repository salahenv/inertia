import React, { useState, useEffect, useRef } from 'react';

const Header = ({
  } : any) => {

  return (
    <div className="font-bold z-100 shadow-2xl text-2xl sticky top-0 bg-neutral-100 px-4 py-2 border-b border-solid border-gray-400 text-blue-600">Inertia</div>
  );
};

export default Header;
