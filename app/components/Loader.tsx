import React, { useState, useEffect } from 'react';

const Loader = ({} : any) => {

    useEffect(() => {
    
    }, []);

    return (
        <div>
            
        </div>
    );
};

export default Loader;