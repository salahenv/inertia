// In your service worker (sw.js), check if there's anything handling the push event
self.addEventListener('push', (event) => {
    // If there's logic here, it may be conflicting with local notifications
  });
  